extends: conditional
message: "'%s' is an acronym with no definition."
level: error
scope: text
ignorecase: false
first: (?:\b[A-Z][a-z]+ )+\(([A-Z]{3,5})\)
second: \b([A-Z]{3,5})\b
# ... with the exception of these:
exceptions:
  - XXX
